# hooks/

Product-side harness hooks. The **active** hook set lives in `.claude/hooks/` (session-start
context, pre-compact memory flush, quality gates, etc.); this directory is the product-facing
mirror for hooks promoted out of the harness layer. See `AGENTS.md` for the directory map.
