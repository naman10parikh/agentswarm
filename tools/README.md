# tools/

Product-side harness tool integrations (CLIs, MCP shims, helper binaries the agent layer can
call). Empty for now; populated as agentswarm grows its own tooling. See `AGENTS.md` for the
directory map.
