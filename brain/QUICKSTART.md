---
type: operations
status: active
created: 2026-05-26
updated: 2026-05-26
tags: [agentswarm, quickstart]
source: ../QUICKSTART.md
related: ["[[MOC - agentswarm]]", "[[README]]"]
---

# QUICKSTART (navigation note)

Navigation note for the getting-started guide. **Source of truth:** [`QUICKSTART.md`](../QUICKSTART.md).

## Summary

The fastest path from clone to a running swarm: install + `pnpm build` + `node dist/ceo.js --help`
for the TypeScript CLI, the `init → validate → run --dry → run` first-swarm flow, the Bash CLI
setup (`install.sh` + `pip3 install pyyaml`), and dev commands (`pnpm test`, `pnpm lint`). All
build+run commands are named inline. For the full schema, see [[README]].

## Related notes

- [[MOC - agentswarm]]
- [[README]]
