# memory/daily/

Per-day session logs (`YYYY-MM-DD.md`). Each day's log is a standalone story of what happened.
Compressed into `memory/archive/` after ~30 days. See `memory/MEMORY.md` for the index.
