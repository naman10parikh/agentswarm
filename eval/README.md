# eval/

Harness evaluation + observer — scoring runs that grade the repo's own outputs so it can
self-improve. Part of the inherited Energy agent-native harness (see `CLAUDE.md` → "Harness
components"). Empty for now; eval suites land here as the harness matures.
